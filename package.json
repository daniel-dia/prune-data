{
  "name": "slimify",
  "version": "0.1.6",
  "description": "Dependency-free recursive payload slimmer.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/daniel-dia/slimify.git"
  },
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "test": "node --test --test-isolation=process slimify.test.js",
    "coverage": "node --test --test-isolation=process --experimental-test-coverage slimify.test.js",
    "mutation": "stryker run",
    "prepack": "npm run build"
  },
  "keywords": [
    "payload",
    "prune",
    "recursive",
    "clean",
    "compact"
  ],
  "license": "MIT",
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260707.2",
    "@stryker-mutator/core": "^10.0.0",
    "typescript": "^5.9.3"
  }
}
